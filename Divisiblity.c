#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number");
    scanf("%d",&n);
    if(n%3==0 && n%5==0)
    {
        printf("%d is divisible by both 3 and 5\n",n);
    }
    else if (n%3==0
    {
        printf("")
    })
    {
        printf("%d is not divisible by 3 or 5\n",n);
    }
    return 0;
}